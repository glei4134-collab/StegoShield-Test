<Window x:Class="StegoShield.Desktop.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="StegoShield Pro"
        Height="700"
        Width="1000"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="CanMinimize">

    <Window.Resources>
        <Style x:Key="NavButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#666666"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="20,12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                Padding="{TemplateBinding Padding}"
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#E8E8F0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="NavButtonSelectedStyle" TargetType="Button" BasedOn="{StaticResource NavButtonStyle}">
            <Setter Property="Background" Value="#667EEA"/>
            <Setter Property="Foreground" Value="White"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#5568D3"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 自定义标题栏 -->
        <Border Grid.Row="0" Background="White" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown" Cursor="SizeAll">
            <Grid Height="48">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- 图标和标题 -->
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="16,0,0,0">
                    <TextBlock Text="🛡️" FontSize="20" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <TextBlock Text="StegoShield Pro" 
                               FontSize="14" 
                               FontWeight="SemiBold" 
                               Foreground="#1A1A2E" 
                               VerticalAlignment="Center"/>
                </StackPanel>

                <!-- 用户信息 -->
                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,8,0">
                    <TextBlock x:Name="txtUserInfo" 
                               Text="未登录" 
                               FontSize="11" 
                               Foreground="#888888"
                               VerticalAlignment="Center"
                               Margin="0,0,12,0"/>
                    <Button x:Name="btnLogout" 
                            Content="登出"
                            Padding="8,4"
                            FontSize="10"
                            Background="Transparent"
                            Foreground="#666666"
                            BorderThickness="0"
                            Cursor="Hand"
                            Click="BtnLogout_Click">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="border" Background="{TemplateBinding Background}"
                                        CornerRadius="4" Padding="{TemplateBinding Padding}">
                                    <ContentPresenter HorizontalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="border" Property="Background" Value="#F0F0F0"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </StackPanel>

                <!-- 关闭按钮 -->
                <Button Grid.Column="3" 
                        Content="✕"
                        Width="48"
                        Height="48"
                        FontSize="16"
                        Background="Transparent"
                        Foreground="#666666"
                        BorderThickness="0"
                        Cursor="Hand"
                        Click="BtnClose_Click">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="border" Background="{TemplateBinding Background}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="border" Property="Background" Value="#FF3B30"/>
                                    <Setter Property="Foreground" Value="White"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </Grid>
        </Border>

        <Grid Grid.Row="1" Margin="24,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="White" CornerRadius="12" Padding="12">
                <StackPanel>
                    <TextBlock Text="功能导航" FontSize="14" FontWeight="SemiBold" 
                               Foreground="#1A1A2E" Margin="8,8,8,16"/>

                    <Button x:Name="btnEncode" 
                            Content="📝 图片隐写" 
                            Style="{StaticResource NavButtonSelectedStyle}"
                            Click="BtnEncode_Click"
                            HorizontalContentAlignment="Left"/>

                    <Button x:Name="btnDecode" 
                            Content="🔍 数据提取" 
                            Style="{StaticResource NavButtonStyle}"
                            Click="BtnDecode_Click"
                            HorizontalContentAlignment="Left"
                            Margin="0,4,0,0"/>

                    <Button x:Name="btnSettings" 
                            Content="⚙️ 设置中心" 
                            Style="{StaticResource NavButtonStyle}"
                            Click="BtnSettings_Click"
                            HorizontalContentAlignment="Left"
                            Margin="0,4,0,0"/>

                    <Button x:Name="btnVip" 
                            Content="👑 VIP中心" 
                            Style="{StaticResource NavButtonStyle}"
                            Click="BtnVip_Click"
                            HorizontalContentAlignment="Left"
                            Margin="0,4,0,0"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="1" Background="White" CornerRadius="12" Margin="16,0,0,0">
                <Grid x:Name="MainContentArea">
                    <!-- 内容将通过代码动态加载 -->
                </Grid>
            </Border>
        </Grid>

        <Border Grid.Row="2" Background="White" Padding="24,12" 
                BorderBrush="#E0E0E0" BorderThickness="0,1,0,0">
            <Grid>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                    <Ellipse x:Name="statusIndicator" Width="8" Height="8" Fill="#34C759" Margin="0,0,8,0"/>
                    <TextBlock x:Name="txtStatus" Text="就绪" FontSize="11" Foreground="#888888"/>
                </StackPanel>

                <TextBlock Text="StegoShield Pro v1.0 | 本地模式" 
                           FontSize="10" 
                           Foreground="#AAAAAA"
                           HorizontalAlignment="Right"
                           VerticalAlignment="Center"/>
            </Grid>
        </Border>
    </Grid>
</Window>
